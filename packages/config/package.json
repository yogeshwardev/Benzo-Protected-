{
  "name": "@benzo/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./tsconfig/base": "./tsconfig/base.json",
    "./tsconfig/next": "./tsconfig/next.json",
    "./tsconfig/nest": "./tsconfig/nest.json"
  },
  "scripts": {
    "build": "echo config package has no build step",
    "lint": "echo config package has no lint step",
    "typecheck": "echo config package has no typecheck step",
    "test": "echo config package has no tests yet"
  }
}

